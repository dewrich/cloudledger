# CloudLedger/mkdocs.yml
# Standalone repo — no platform-docs aggregator in this POC, so there is no
# base to INHERIT. MkDocs is retired anyway: GitHub renders the shipped docs
# (Markdown + Mermaid) natively. This file is vestigial, kept only so tooling
# that still expects it does not trip.

site_name: CloudLedger
site_url: https://example.com/cloudledger/
